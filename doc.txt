This is line 1 in the document.
First 'real' commit: added a line.

To illustrate merge:
  - Line added in the remote repository.
  - Line added in the local repository.

Now we illustrate the merge again!
  - Line added in the remote repo.
  - Line added in the local repo.

Now we illustrate the merge again!
  - Line added in the remote repo.
  - Line added in the local repo.